name = "wake-api"
main = "index.js"
compatibility_date = "2024-01-01"

[[kv_namespaces]]
binding = "WAKE_QUEUE"
id = "e3cd3b3b7bbe424e8843057e56309e28"

# Secrets (set via `wrangler secret put`):
#   ESP32_SECRET  - shared secret for the ESP32 (/check, /ack) and the PC status push (/status)
